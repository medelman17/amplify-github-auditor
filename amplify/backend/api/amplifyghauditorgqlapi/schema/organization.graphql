type GitHubOrganization @model @key(fields: ["id", "login"]) @aws_api_key {
  id: ID!
  createdAt: AWSDateTime!
  updatedAt: AWSDateTime!
  login: String!
  nodeId: String!
  apiUrl: AWSURL!
  avatar: AWSURL
  email: AWSEmail
  description: String
}
